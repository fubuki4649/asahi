#!/bin/sh

# asahictl - CLI tool for managing system settings
# Usage: asahictl [command] [value]

SCRIPT_NAME="asahictl"
VERSION="1.0.0"

DBUS_DEST="org.freedesktop.impl.portal.desktop.asahi"
DBUS_PATH="/org/freedesktop/portal/desktop"
DBUS_IFACE="org.freedesktop.impl.portal.asahi.Control"

# Function to display help information
show_help() {
    cat << EOF
$SCRIPT_NAME - System Dark Mode CLI Utility

USAGE:
    $SCRIPT_NAME [command] [value]

COMMANDS:
    override [value]              Set or clear manual dark mode override
                                  Values: off, dark, light, nopref
    query [target]                Query daemon state and telemetry
                                  Targets: all, theme, today, location
    update                        Force an immediate location refresh and theme rebroadcast

EXAMPLES:
    $SCRIPT_NAME override off         Disable manual override and determine mode automatically
    $SCRIPT_NAME override dark        Manually force dark mode
    $SCRIPT_NAME override light       Manually force light mode
    $SCRIPT_NAME override nopref      Manually force no system preference
    $SCRIPT_NAME query all            Print a summary of the current live state
    $SCRIPT_NAME query theme          Print the current theme and override status
    $SCRIPT_NAME query today          Print today's expected sunrise and sunset times
    $SCRIPT_NAME query location       Print the location currently used for calculations

OPTIONS:
  -h, --help                      Show this help message
  -v, --version                   Show version information

EOF
}

# Function to display version
show_version() {
    echo "$SCRIPT_NAME version $VERSION"
}

# Function for manual override
set_override() {
    case "$1" in
        "off")    int_val=-1 ;;
        "dark")   int_val=1 ;;
        "light")  int_val=2 ;;
        "nopref") int_val=0 ;;
        "")
            echo "Error: override requires a value (off, dark, light, nopref)" >&2
            return 1
            ;;
        *)
            echo "Error: Invalid value '$1' for override. Valid values are: off, dark, light, nopref" >&2
            return 1
            ;;
    esac

    echo "Setting manual override to: $1"

    if ! reply=$(busctl --user call -- "$DBUS_DEST" "$DBUS_PATH" "$DBUS_IFACE" setManualDarkMode i "$int_val" 2>&1); then
        echo "Error: Failed to set manual override: $reply" >&2
        return 1
    fi

    echo "Manual override successfully set to $1"
}

# Queries a single property on the Control D-Bus interface using busctl
_busctl_get() {
    busctl --user get-property "$DBUS_DEST" "$DBUS_PATH" "$DBUS_IFACE" "$1" 2>&1
}

# Queries the currentTheme and isOverrideSet properties
query_theme() {
    if ! theme_reply=$(_busctl_get "currentTheme"); then
        echo "Error: Failed to query currentTheme: $theme_reply" >&2
        return 1
    fi
    theme_val="${theme_reply#* }"

    case "$theme_val" in
        0) theme_name="No Preference" ;;
        1) theme_name="Dark Mode" ;;
        2) theme_name="Light Mode" ;;
        *) theme_name="Unknown ($theme_val)" ;;
    esac

    if ! override_reply=$(_busctl_get "isOverrideSet"); then
        echo "Error: Failed to query isOverrideSet: $override_reply" >&2
        return 1
    fi
    is_override="${override_reply#* }"

    if [ "$is_override" = "true" ]; then
        override_str="override set"
    else
        override_str="automatic"
    fi

    echo "Theme: $theme_name ($override_str)"
}

# Queries the todayTransitionTimes property (RFC 3339 timestamps of today's sunrise and sunset)
query_today() {
    if ! reply=$(_busctl_get "todayTransitionTimes"); then
        echo "Error: Failed to query todayTransitionTimes: $reply" >&2
        return 1
    fi
    # busctl formats a (ss) struct as: (ss) "sunrise_val" "sunset_val"
    stripped="${reply#*(ss) }"
    sunrise="${stripped%%\"*}"
    stripped="${stripped#*\"}"
    sunrise="${stripped%%\"*}"
    stripped="${stripped#*\" \"}"
    sunset="${stripped%%\"*}"

    echo "Sunrise: $sunrise"
    echo "Sunset:  $sunset"
}

# Queries the location property (lat/lon currently used for sunrise/sunset calculations)
query_location() {
    if ! reply=$(_busctl_get "location"); then
        echo "Error: Failed to query location: $reply" >&2
        return 1
    fi
    lat_lon="${reply#*(dd) }"
    lat="${lat_lon%% *}"
    lon="${lat_lon#* }"

    echo "Location: lat=$lat, lon=$lon"
}

# Prints a combined summary of all query targets
query_all() {
    query_theme || return 1
    query_today || return 1
    query_location || return 1
}

# Dispatcher for query command
query_daemon() {
    case "$1" in
        "all")      query_all ;;
        "theme")    query_theme ;;
        "today")    query_today ;;
        "location") query_location ;;
        "")
            echo "Error: query requires a target (all, theme, today, location)" >&2
            return 1
            ;;
        *)
            echo "Error: Invalid target '$1' for query. Valid targets are: all, theme, today, location" >&2
            return 1
            ;;
    esac
}

# Forces an immediate location refresh and theme rebroadcast via D-Bus
force_update() {
    if ! reply=$(busctl --user call -- "$DBUS_DEST" "$DBUS_PATH" "$DBUS_IFACE" forceUpdate 2>&1); then
        echo "Error: Failed to force update: $reply" >&2
        return 1
    fi
    echo "Update triggered"
}

# Main logic
main() {
    case "$1" in
        "-h"|"--help"|"help")
            show_help
            exit 0
            ;;
        "-v"|"--version"|"version")
            show_version
            exit 0
            ;;
        "override")
            set_override "$2"
            exit $?
            ;;
        "query")
            query_daemon "$2"
            exit $?
            ;;
        "update")
            force_update
            exit $?
            ;;
        "")
            echo "Error: No command specified" >&2
            echo
            show_help
            exit 1
            ;;
        *)
            echo "Error: Unknown command '$1'" >&2
            echo
            show_help
            exit 1
            ;;
    esac
}

main "$@"